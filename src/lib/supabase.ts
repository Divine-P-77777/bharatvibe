import { createClient } from './supabase/client'; // Import the createClient function from client.ts

// Use the createClient function to get the Supabase client instance
export const supabase = createClient();
